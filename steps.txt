

For A1.1 (./app/)

> cd ./app/

# docker build . -t <username>/node-web-app
> docker build . -t yuunokun/node-web-app

# docker run -p <public-port>:8080 -d <username>/node-web-app
# Running your image with -d runs the container in detached mode, leaving the 
# container running in the background. The -p flag redirects a public port to a 
# private port inside the container.
> docker run -p 3000:8080 -d yuunokun/node-web-app

# http://localhost:3000

> docker ps
CONTAINER ID   IMAGE                   COMMAND                  CREATED              STATUS          PORTS                    NAMES
986dfc432820   yuunokun/node-web-app   "docker-entrypoint.s…"   About a minute ago   Up 29 seconds   0.0.0.0:3000->8080/tcp   pensive_kepler


# docker kill <Container-ID>
> docker kill 986dfc432820


For A1.2 (./nginx-sample/)

> cd ./nginx-sample/
> docker build . -t otota12
> docker run -p 80:80 otota12


For A1.3 (./nginx/)

> cd ./
> docker-compose up --build

# http://localhost:3000
# port 80, 5000, 8080 does not work.